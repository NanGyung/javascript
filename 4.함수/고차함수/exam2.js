{
    {
        // 배열을 입력받아 2배수 한 배열을 반환하는 함수를 구현하시오
        const arr = [1,2,3];
        function x(arr){
            const array = arr.slice();
            for(let i = 0; i < array.length;i ++){
                array[i] *= 2;
            }
            return array;
        }
        console.log(x(arr));   
    }

    {
        // 배열을 입력받아 3배수 한 배열을 반환하는 함수를 구현하시오
        function x(arr){
            const array = arr.slice();
            for(let i = 0; i < array.length;i ++){
                array[i] *= 3;
            }
            return array;
        }
        console.log(x(arr));   
    }
}

{
    const arr = [1,2,3];
    function x(arr){
        for(let i = 0; i < arr.length;i ++){
            array[i] *= 3;
            }
        }
}
